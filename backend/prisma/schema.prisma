generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model User {
  id        String   @id
  email     String   @unique
  password  String
  createdAt DateTime
  updatedAt DateTime
}
